{
  "name": "paywall-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo \"Nada para buildar\"",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.0.0"
  },
  "engines": { "node": ">=18" }
}
